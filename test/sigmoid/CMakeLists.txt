# The regex would be too complicated in this case, just check that it does not crash.
# If some problems happen run test_sigmoid manually and visually inspect the output.
make_test(
    test_sigmoid
    "NO_REGEX")